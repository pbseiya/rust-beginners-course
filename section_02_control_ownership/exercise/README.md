# แบบฝึกหัด: โปรแกรมคำนวณราคาสินค้า 🛒

เขียนโปรแกรมคำนวณราคารวมของสินค้าสองชนิดในตะกร้า แสดงผลลัพธ์สุดท้ายออกทางหน้าจอ

1. **สร้างฟังก์ชันชื่อ `calculate_item_total`** ซึ่งทำหน้าที่คำนวณราคารวมของสินค้าหนึ่งชนิด
   - ฟังก์ชันนี้ต้องรับพารามิเตอร์ 2 ตัวคือ:
     - `price` (ราคาต่อชิ้น) ให้ใช้ชนิดข้อมูลเป็น `f64`
     - `quantity` (จำนวนชิ้น) ให้ใช้ชนิดข้อมูลเป็น `u32`
   - ฟังก์ชันนี้ต้องคืนค่า (return) ราคารวมของสินค้านั้น (`price` * `quantity`) ซึ่งควรเป็นชนิด `f64`

2. **ในฟังก์ชัน `main`** ให้ทำสิ่งต่อไปนี้
   - สร้าง **ตัวแปร** เพื่อเก็บข้อมูลของสินค้าชิ้นที่หนึ่ง (เช่น แอปเปิ้ล) โดยกำหนดราคา 15.50 และจำนวน 5 ชิ้น
   - สร้าง **ตัวแปร** เพื่อเก็บข้อมูลของสินค้าชิ้นที่สอง (เช่น กล้วย) โดยกำหนดราคา 8.75 และจำนวน 8 ชิ้น
   - เรียกใช้ฟังก์ชัน `calculate_item_total` เพื่อคำนวณราคารวมของสินค้าแต่ละชิ้น และเก็บผลลัพธ์ไว้ในตัวแปรใหม่
   - รวมราคาสินค้าทั้งสองชนิดเพื่อหา **ยอดรวมสุดท้าย**
   - ใช้ `println!` เพื่อแสดงผลยอดรวมสุดท้ายในรูปแบบที่สวยงาม
